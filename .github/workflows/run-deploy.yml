name: Deploy to Elastic Beanstalk
on:
    workflow_dispatch:
jobs:
  build:
    name: Build Application
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build
        run: yarn build
      
      - name: Make envfile
        uses: SpicyPizza/create-envfile@v2.0
        with:
          envkey_ENV: "production"
          envkey_APP_NAME: "api-fast-order"
          envkey_JWT_SECRET: ${{ secrets.JWT_TEMPLATE_PUBLIC }}
          file_name: .env.production
          fail_on_empty: false
          sort_keys: false

      - name: Copy env
        run: ls && cp .env.production dist
      
      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
            name: artifact
            path: |
              *
              .env.production
              !src
              !test
              !node_modules

  # deploy-prod:
  #   name: Deploy PROD Environment
  #   runs-on: ubuntu-latest
  #   needs: build
  #   steps:
  #     - name: Download build artifact
  #       uses: actions/download-artifact@v4
  #       with:
  #           name: artifact
  #           path: artifact
  #     - name: Zip Artifact for Deployment
  #       run: |
  #         cd artifact
  #         zip -r ../service.zip ./*
  #     - name: Deploy to Elastic Beanstalk
  #       uses: einaregilsson/beanstalk-deploy@v22
  #       with:
  #         aws_access_key: ${{ secrets.AWS_ACCESS_KEY_ID }}
  #         aws_secret_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  #         application_name: prod-api-portal-do-criador
  #         environment_name: prod-api-portal-do-criador
  #         region: ${{ secrets.AWS_REGION }}
  #         version_label: ${{ github.run_id }}
  #         deployment_package: service.zip
  #         use_existing_version_if_available: true